export type user = {
    id: string | number | undefined | number,
    name: string,
    email: string,
    password: string
}

export type product = {
    id: string | number | undefined | number, 
    name: string,
    price: string,
    image_url: string
}